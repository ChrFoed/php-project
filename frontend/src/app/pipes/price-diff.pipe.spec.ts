import { PriceDiffPipe } from './price-diff.pipe';

describe('PriceDiffPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceDiffPipe();
    expect(pipe).toBeTruthy();
  });
});
